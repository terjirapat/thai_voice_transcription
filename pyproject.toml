[project]
name = "thai-voice-transcription"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "typhoon-asr>=0.1.0",

]

[dependency-groups]
dev = [
    "ipykernel>=7.1.0",
]
